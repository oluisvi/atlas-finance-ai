import "reflect-metadata";
import { ExportsService } from "./exports.service.js";
import { ExportFormat, ExportReportType } from "./exports.constants.js";
import { CsvExportRenderer } from "./renderers/csv-export.renderer.js";
import { XlsxExportRenderer } from "./renderers/xlsx-export.renderer.js";
import { PdfExportRenderer } from "./renderers/pdf-export.renderer.js";
describe("ExportsService", () => { const reports = { summary: jest.fn(() => Promise.resolve({ period: {}, income: "0", expenses: "0", netCashFlow: "0" })), cashFlow: jest.fn(() => Promise.resolve({ data: [] })), incomeExpense: jest.fn(() => Promise.resolve({ period: {}, data: [] })), categories: jest.fn(() => Promise.resolve({ data: [] })), netWorth: jest.fn(() => Promise.resolve({ period: {}, data: [] })), goals: jest.fn(() => Promise.resolve({ data: [] })), budgets: jest.fn(() => Promise.resolve({ data: [] })), accounts: jest.fn(() => Promise.resolve({ data: [] })), recurring: jest.fn(() => Promise.resolve({ data: [] })), monthly: jest.fn(() => Promise.resolve({ data: [] })), yearly: jest.fn(() => Promise.resolve({ data: [] })) }; const audit = { record: jest.fn(() => Promise.resolve()) }; const service = new ExportsService(reports, audit, new CsvExportRenderer(), new XlsxExportRenderer(), new PdfExportRenderer()); it("delegates to reports and records safe export metadata", async () => { const file = await service.export("user", ExportReportType.SUMMARY, { format: ExportFormat.CSV, currency: "BRL" }); expect(reports.summary).toHaveBeenCalledWith("user", expect.objectContaining({ currency: "BRL" })); expect(file.contentType).toContain("text/csv"); expect(audit.record).toHaveBeenCalledWith(expect.objectContaining({ action: "reports.export" })); }); });
