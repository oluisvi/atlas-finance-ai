import { EmergencyFundInsightsDetector } from "./emergency-fund-insights.detector.js";import { context,money } from "./detector-test.fixture.js";
describe("EmergencyFundInsightsDetector",()=>{const detector=new EmergencyFundInsightsDetector();const goal=(current:number,target=100,status="ACTIVE",planId:string|null="plan")=>({id:"goal",type:"EMERGENCY_FUND",currency:"BRL",current:money(current),target:money(target),targetDate:null,status,deleted:false,planId});it("detects missing configuration",async()=>expect((await detector.detect(context()))[0]?.code).toBe("EMERGENCY_FUND_MISSING"));it.each([[0,"EMERGENCY_FUND_LOW"],[49,"EMERGENCY_FUND_LOW"],[50,undefined],[99,undefined],[100,"EMERGENCY_FUND_COMPLETED"],[120,"EMERGENCY_FUND_COMPLETED"]])("maps progress %s",async(current,code)=>expect((await detector.detect(context({goals:[goal(current)]})))[0]?.code).toBe(code));it("honors completed status",async()=>expect((await detector.detect(context({goals:[goal(10,100,"COMPLETED")]})))[0]?.code).toBe("EMERGENCY_FUND_COMPLETED"));it("ignores deleted and other-currency records",async()=>{const deleted={...goal(1),deleted:true};const usd={...goal(1),currency:"USD"};expect((await detector.detect(context({goals:[deleted,usd]})))[0]?.code).toBe("EMERGENCY_FUND_MISSING")});it("uses safe string metadata",async()=>expect(JSON.stringify(await detector.detect(context({goals:[goal(10)]})))).not.toMatch(/userId|fingerprint|transactions/));});
