import{describe,it,expect,vi,beforeEach}from"vitest";import{api,ApiError,session}from"./client";
describe("api client",()=>{beforeEach(()=>{session.set(null);vi.restoreAllMocks()});it("maps structured errors",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue(new Response(JSON.stringify({statusCode:429,code:"RATE_LIMIT",message:"Espere"}),{status:429,headers:{"content-type":"application/json"}})));await expect(api("/auth/login",{auth:false})).rejects.toBeInstanceOf(ApiError)});it("parses JSON",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue(new Response(JSON.stringify({ok:true}),{status:200,headers:{"content-type":"application/json"}})));await expect(api("/health",{auth:false})).resolves.toEqual({ok:true})})});
