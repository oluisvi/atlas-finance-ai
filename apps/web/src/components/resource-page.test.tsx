import{describe,it,expect,vi}from"vitest";import{render,screen}from"@testing-library/react";import{QueryClient,QueryClientProvider}from"@tanstack/react-query";import{ResourcePage}from"./resource-page";
vi.mock("@/lib/api/client",()=>({api:vi.fn().mockResolvedValue([]),ApiError:class extends Error{}}));describe("resource page",()=>it("shows contextual empty state",async()=>{render(<QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}><ResourcePage kind="accounts"/></QueryClientProvider>);expect(await screen.findByText("Você ainda não possui contas.")).toBeInTheDocument()}));
