"use client";import{ResponsiveContainer,AreaChart,Area,XAxis,YAxis,CartesianGrid,Tooltip}from"recharts";
function toRows(data:unknown){const source=Array.isArray(data)?data:data&&typeof data==="object"&&"data"in data&&(Array.isArray((data as {data:unknown}).data))?(data as {data:Record<string,unknown>[]}).data:[];return source.map((r:Record<string,unknown>)=>({label:String(r.date??r.period??""),income:Number(r.income??0),expense:Number(r.expenses??r.expense??0)}))}
export default function DashboardChart({data}:{data:unknown}){const rows=toRows(data);if(!rows.length)return <div style={{height:260,display:"grid",placeItems:"center",color:"var(--muted)"}}>Ainda não há dados suficientes para o gráfico.</div>;return <div style={{height:280,width:"100%"}}><ResponsiveContainer><AreaChart data={rows}><CartesianGrid stroke="#e7ebf0" vertical={false}/><XAxis dataKey="label" tick={{fontSize:11}}/><YAxis tick={{fontSize:11}}/><Tooltip/><Area type="monotone" dataKey="income" stroke="#078c5c" fill="#078c5c18" strokeWidth={2}/><Area type="monotone" dataKey="expense" stroke="#d94141" fill="#d9414111" strokeWidth={2}/></AreaChart></ResponsiveContainer></div>}

